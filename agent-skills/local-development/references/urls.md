# Local Development: URLs

- `/` — speaker list
- `/speakers/<id>` — speaker detail
- `/admin` — register transcripts, annotate
- `/docs` — API docs

For more on env vars, database, and deployment, see:

- `doc/WEBAPP.md`
- `doc/HOWTO.md`

